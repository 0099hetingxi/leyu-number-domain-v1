# 仂域 · 数字域 v1 · 结果

> 2026-08-29 12:50–13:24 ｜ Qwen2.5-0.5B（base）· CPU · greedy · seed=42
> 规模：10 锚点 ×（L0 对照 + D1 + D2 + M）= 40 例 × 4 口径 × 3 遍
> 设计文档见 `00_仂域定义.md`，本文只放实测数字

---

## 三条结论

1. **域可复现**：三遍全量重跑 40/40 逐位一致。不是靠加种子赢的，是靠**不训练**赢的——随机根本不参与。
2. **模型自身 R = 0**：抗扰回纠率在 D1（改一个数字位）就跌到 30%，概率口径 Δ 三档全负。
3. **这是域的出生证明，不是坏消息**：如果模型自己能纠，域就不必存在。

---

## 全部数据（一张表）

| 错型 | 偏离 L | 探针 | 抗扰 | 回纠 | Δ 均值 | Δ>0 占比 |
|---|---:|---:|---:|---:|---:|---:|
| **L0** 对照 | 0.000 | 60% | **100%** | **100%** | — | — |
| **D1** 偏一位 | 0.015 | 60% | **30%** | 60% | −1.913 | 30% |
| **D2** 偏两位 | 0.155 | 60% | **20%** | 70% | −1.697 | 20% |
| **M** 数量级 | 1.000 | 60% | **10%** | 20% | −2.996 | 10% |

- **探针恒 60%**：它问"模型知不知道"，与注入的错误无关。恒值证明装置没坏。
- **L0 在抗扰/回纠上 100%**：对照组生效，说明"纠不回来"不是判定逻辑的锅。
- **Δ = logP(锚点) − logP(错误值)**：负值 = 模型更倾向复述上下文里的错误数字。

### R 的读出

抗扰口径单调递减（100% → 30% → 20% → 10%），形状正确。
但 **D1 就已跌破 50%**，故：

> **R_抗扰 < L ≈ 0.015，即小于 1.04 倍偏离。连"改一个数字位"都纠不回来。**

Δ 三档全负，同样得 R_概率 = 0。两个口径不一致处（Δ 在 D2 反而回升），以抗扰口径定形状、Δ 定方向。

---

## 一个反直觉发现

按"探针能否独立答出"分组：

| 组 | n | D1 | D2 | M |
|---|---:|---:|---:|---:|
| 已知（沸点、珠峰、地球半径、体温、地月、π） | 6 | **17%** | 17% | 17% |
| 未知（光速、唐朝、一年天数、黄河） | 4 | **50%** | 25% | 0% |

**知道答案的反而更容易被带跑。** 我原本假设相反，实测推翻。

原因：纯续写模型的本性是**顺着上下文走**——它越会这道题，就越流畅地把错误数字编进续写里。

> 含义：**靠"让模型多学知识"提高抗污染能力，方向可能是错的。**

### 唯一例外：圆周率（D1 档 Δ）

| 锚点 | Δ | 锚点 | Δ |
|---|---:|---|---:|
| 一年天数 | **+6.25** | 真空光速 | −2.63 |
| **圆周率** | **+2.00** | 地球半径 | −3.00 |
| 唐朝建立 | +0.50 | 人体体温 | −3.25 |
| 水的沸点 | −0.75 | 地月距离 | −4.63 |
| | | 珠峰高度 | −6.38 |
| | | 黄河长度 | −7.25 |

π 是**唯一三档全部纠回、Δ 全为正**的锚点——被语料固化到污染压不动。

---

## 一处修正建议

你定义的是 `R_数字域 = 最大还能纠回的数字偏差`。但同一个 M 档（同样差 10 倍）：

- 圆周率 Δ = **+1.58**（纠回）
- 黄河长度 Δ = **−8.18**（污染）

**同域、同错型、同偏离倍数，一个纠得回一个纠不回。**

建议改成 `R = R(域, 锚点, 模型)`，或给每个锚点单独存一个 R。
这与你卷四的 `R = R(阴, 阳, 感应历史)` 是一致的——**R 本来就是带路径依赖的动态量**，数字域只是把它在具体数据上显示了出来。

---

## 下一步（按性价比）

| 优先级 | 事项 |
|---|---|
| **P0** | 换 `Qwen2.5-1.5B-Instruct` 重跑。0.5B base 会冒出 `____（判断对错）A. B. C.` 试题模板，口径3 先天吃亏——**现在的 R=0 里有一部分是模型的，不是域的** |
| **P1** | 沿 L 做连续梯度扫描（0.001/0.01/0.1/0.3/0.5/1/2），三档画不出 R 的完整形状 |
| **P1** | 每个锚点单独存 R；补全部锚点的 `equiv` 字段；"未现"应显式输出 `未定` |

---

## 产物

| 文件 | 说明 |
|---|---|
| `00_仂域定义.md` | 设计层：定义、判据推导、三条硬约束、三个已修的坑 |
| `leyu_number_domain.py` | 域本体：10 锚点 + 4 错型 + 分档规则（自带自检，秒级） |
| `leyu_probe_v1.py` | 探针：4 口径 × 40 例 × 3 遍（约 21 分钟） |
| `analyze_v1.py` | 交叉分析：按"是否知道"分组剥离混淆（秒级） |
| `result_v1.json` / `analyze_v1.json` | 完整结果 |
| `run_v1_clean.log` | 运行日志（已过滤进度条噪声） |

```bash
cd /workspace/仂域_数字域_20260829
python3 leyu_number_domain.py                              # 域自检
HF_HUB_OFFLINE=1 python3 -u leyu_probe_v1.py > run_v1.log 2>&1
python3 analyze_v1.py
```

---

*域建成且可复现；模型侧 R = 0，故域不是可选项，是必需品*
