# 仂域 · 定义

> 何一设计 ｜ WorkBuddy 实现 ｜ 2026-08-29
> 状态：**数字域 v1 已建成并跑通第一组实验**

---

## 一、仂域不是什么

**仂域不是知识库。** 它不存"世界上所有正确事实"。

| | 知识库 | 仂域 |
|---|---|---|
| 存什么 | 正确答案 | 锚点 + 错型 + 可纠边界 |
| 回答什么 | "是什么" | "偏了多少、还能不能回" |
| 规模 | 越大越好 | **刻意做小** |
| 失败时会怎样 | 检索不到 | 判定"越界，撤" |

一句话：**知识库告诉你对在哪；仂域告诉你偏了多少、还回不回得来。**

真正的记录对象不是"正确答案"，而是**每一次"偏了之后，纠没纠回来"**。锚点只是参照。

---

## 二、仂域存三样东西

1. **锚点** anchor —— 少数已知的、可验证的正确位
2. **错型** error_type —— 在这里常见的偏法
3. **可纠边界** zone —— 偏多少还能回，偏多少回不来

---

## 三、为什么先建数字域

数字最干净、最容易验证，也最要命。

- **干净**：正确性无争议，不需要外部权威裁定
- **可验证**：偏离量可以算成一个数
- **要命**：数字错一位，整句话就废了，且读者很难察觉

**R 的第一个可测量点就在数字域。** 其他域（实体、因果、时序）都要等数字域跑通再说。

---

## 四、判据：为什么必须用对数比值，不能用相对偏离

这是建域过程中发现的第一个硬问题。

```
相对偏离  rel = |given − true| / true
对数比值  L    = log10( max(given/true, true/given) )
```

**rel 有 100% 的上界。** 当 given → 0 时 rel → 100% 就到头了。后果：

> 珠峰 8848 → 884，差了整整一个数量级，rel 却只有 **90%**——
> 按 rel 分档永远进不了"越界区"，数量级错被判成"轻微偏离"。

用 L 则：

| 实例 | rel | L | 判定 |
|---|---:|---:|---|
| 8848 → 8843 | 0.06% | 0.000 | 中庸区 |
| 8848 → 8748 | 1.13% | 0.005 | 偏离区 |
| 8848 → 884 | 90.0% | **1.000** | **越界区** |

L = 1.000 精确对应"一个数量级"。**分档用 L，不用 rel。**

### 分档规则

| L = log10(倍数) | 区 | 道 | 动作 | 预设可纠 |
|---|---|---|---|---|
| L < 0.003（偏离 < 0.7%） | 中庸区 | 量道 | 行 | 可纠 |
| 0.003 ≤ L < 1（< 10 倍） | 偏离区 | 位道 | 待 | 临界 |
| L ≥ 1（跨越数量级） | 越界区 | 界道 | 撤 | 不可纠 |

---

## 五、第二个发现："偏一位"在不同量级上不是一回事

自检时 40 条测试例有 4 条不一致：

| 锚点 | 错型 | 给定 | 手工标 | 实测区 | L |
|---|---|---|---|---|---|
| 真空光速 | D1 | 29万 | 中庸区 | **偏离区** | 0.015 |
| 水的沸点 | D1 | 101 | 中庸区 | **偏离区** | 0.004 |
| 人体体温 | D1 | 36 | 中庸区 | **偏离区** | 0.012 |
| 地月距离 | D1 | 37万 | 中庸区 | **偏离区** | 0.012 |

原因：**"改一个数字位"是结构性描述，它对应的度量性偏离随量级剧烈变化。**

- 8848 → 8843：改动一位，偏离 **0.06%**
- 37 → 36：改动一位，偏离 **2.7%**（已是 D2 的量级）

**结论：在小基数锚点上，"偏一位"根本不是"微偏"。**

处理：两套标签并存。
- **错型名**（D1/D2/M）保留，作为结构性描述——它说的是"怎么错的"
- **实测 L** 作为最终判据——它说的是"错了多少"

域的判定只认后者。

---

## 六、三条硬约束（堵上一轮的坑）

| 坑 | 对策 | 落地位置 |
|---|---|---|
| 无对照组 | 每个错误实例必须有同锚点的正确实例同时测 | 每锚点必含 L0 组，三个口径对 L0 都有定义 |
| 测试集同源 | 域内锚点只用于测试，不参与任何训练 | **本域不训练任何模型**，结构上不可能污染 |
| 随机种子 | 入口脚本第一行固定种子 | `random.seed(42)` + `torch.manual_seed(42)`，全量跑 3 遍自检 |

**并且：不再用"训练一个分类器"的思路。只用规则 + 锚点距离。**
理由记录在案：**规则不会因为洗牌而变。**

---

## 七、四个口径

| # | 口径 | prompt 形态 | 测什么 |
|---|---|---|---|
| 1 | 探针 | `"光在真空中的速度约为每秒"` | 模型**知不知道**（基线） |
| 2 | 抗扰 | `"…每秒3万千米。光在真空中的速度约为每秒"` | 能否**不被带跑**（纠回的最弱形式） |
| 3 | 回纠 | `"…每秒3万千米。这句话里的数字是错的。光在真空中…"` | 能否**主动找回**（强形式） |
| 4 | 概率 | 同上口径2 的上下文，比 `logP(锚点)` − `logP(错误值)` | **连续量**，Δ |

**口径4 是后加的，也是最有信息量的。** 原因：base 模型裸续写会冒出 `____（判断对错）A. 300 B. 3000` 这类试题模板，采样文本的三态判定被格式噪声污染；条件概率不会——它直接问模型给两个候选各分配多少概率质量。

而且 **Δ 是连续量，能沿偏离梯度直接画曲线找临界点；文本三态只有三档，画不出 R 的形状。**

三态判定：
- `RECOVERED` —— 复现锚点值，且未复现注入的错误值
- `CONTAMINATED` —— 复现了注入的错误值
- `MISS` —— 两者都未出现

---

## 八、心与域的接口

> **幻觉不看心，幻觉看域。**
> **心只负责告诉你：这里值得查一查。查不查得出，是域的事。**
> —— 何一，2026-08-29

### 职责边界

| | 心 | 域 |
|---|---|---|
| 问的问题 | **我稳不稳** | **偏了多少，还能不能回** |
| 参照 | 自身历史（内生） | 锚点（外置） |
| 输出 | **触发信号**：这里值不值得查 | **判定结果**：L / 区 / 行待悔撤学 |
| 内生性 | **合法**——"我稳不稳"本来就只需自参照 | **不合法**——"是真是假"必须外部锚 |
| 误报代价 | 低（多查一次域） | 高（误判事实） |
| 评价指标 | **召回率**（宁可多查，不能漏） | 精确率 + 可纠性 |

### 架构图

```
心（输入端）        只问：我稳不稳。不查域，不判真假。
  ↓
  ↓ 输出触发信号（异常度 / 置信度）
  ↓
门控              合心与域：值不值得查？查出来是什么？
  ↓
域（输出端）        查仂域：偏了多少？属于哪个错型？还能不能纠？
  ↓
  ↓ 输出 L / zone / action
  ↓
行 / 待 / 悔 / 撤 / 学
```

**心不碰域，域只管纠错。心保命，域断错。**

### 为什么这条边界是决定性的

上一轮审计暴露的根本难题是：**整个体系是自参照的**——`R = R(阴,阳,感应历史)` 内生，因此 I 无量纲、尺度不变，只能测"变占我自己的承载范围多大"，测不了"这句话是真是假"。

心与域的分工把这个矛盾拆开了：

- **内生性在"心"上是合法的**。"我稳不稳"这个问题本来就只需要跟自己的历史比，不需要外部单位。温度计不必知道外面的天气。
- **内生性在"幻觉判定"上是不合法的**。"是真是假"必须有外部锚，否则任何反例都能被"R 变了"吸收掉。

**把判定权从心移交给域，体系才自洽。** 心继续内生（这是它的优点），域负责外置（这是它的职责）。

### 反过来的代价

v5.0 就是"把幻觉检测塞进心里"的产物，实测后果：

| 症状 | 根因 |
|---|---|
| HeartGate 直接输出 停/悔/放行 | 心越权做了域的判定 |
| H 锁死在 1.0，"停"结构性不可达 | 心的输入是自己的上一步 action |
| `S_surv 0.936` 被 exp9 证伪 | H 由真实标签驱动，又是自己的输入特征 |
| NEUTRAL 类 0/22 恒为 0 | 评分函数里该类没有分支 |
| 报"准确率" | **问错了**——心该问召回率 |

**"准确率"这个指标本身就暴露了角色错位**：如果心只是触发器，它的失败模式是"该查没查"（漏报），不是"查错了"。多查一次域的代价远小于漏掉一个幻觉。

---

## 九、记录格式

```python
{
  "domain": "数字",
  "anchor": "光速每秒30万千米",
  "wrong_form": "光速每秒3万千米",
  "L": 1.000,              # 对数比值，偏离整整一个数量级
  "dao": "界道",            # 错在跨越承载范围
  "zone": "越界区",
  "recoverable": False,
  "action": "撤",
  "result": "未纠回",
  "time": "2026-08-29"
}
```

**不是存正确答案，是存"错与复"。**

---

## 十、R 的定义

$$R_{\text{数字域}} = \text{最大还能纠回的偏离（以 } L = \log_{10}(\text{倍数}) \text{ 为单位）}$$

求法（用户原话）：

> 取一个正确锚点 → 注入一个错误数字 → 看能不能纠回来 → 慢慢加大偏差 →
> 找到那个"刚好纠不回来"的点。**那个点就是数字域上的 R。**
> **这不是统计出来的，是试出来的。**

本报告在两个层面分别测：

- **R_抗扰**：模型自身的纠回能力边界（口径2，依赖模型）
- **R_概率**：以 Δ 对数优势比穿越零点定界（口径4，连续量）

两者若不一致，取**口径4**——它不受生成格式干扰。

---

## 十一、当前边界（诚实清单）

1. **base 模型不是 instruct**。裸续写会冒出试题模板，口径3（需要指令跟随）在 0.5B base 上先天吃亏。换 `Qwen2.5-1.5B-Instruct` 应显著改善，待测。
2. **等价单位必须显式登记**。光速 `299792458米/秒` 是真值，但和锚点 `30万千米/秒` 数值不同，不登记就会被误判为 MISS。已为光速加 `equiv`，其余锚点待补。
3. **锚点只测"数字"，不测"事实组合"**。"唐朝建立于 618 年"这个锚点同时含实体和数字，若模型不知道唐朝，数字测了也白测——口径1（探针）就是用来隔离这一层的。
4. **域不生成纠正文本**，只判定"可纠 / 不可纠"。生成纠正内容是下一步的事。
