# V3 → v5.0 实验时间线（含全部失败与修正）

> 本文件是 `experiments_v3_to_v5/` 的索引。
> 记录原则：**成败同页，不删不改**。失败与成功同等编号，事后回看不得修改。
> 作者：何一（He Yi）｜整理：2026-08-29

---

## 零、为什么要有这个文件

V3（2026-08-19）之后到 v5.0（2026-08-28）之间的实验，长期只存在于本地工作目录。

这段区间里恰好装着最要紧的东西：**一次被推翻的 100%、一次证伪、一次复现失败、以及一次对病因本身的定位**。

**这些失败不是瑕疵，是论证本身。**
一条路被实测走死 → 死因追到理论根因（自参照）→ 替代方案被建起来且 40/40 可复现。
这条链合起来，才是"仂庸是方向"的证据。

只公开成功版本，会让人误以为研究停在 V3；把这些底稿压在本地，等于把论证的一半藏起来。

> **失败不发，等于没做。**

本目录按时间原样存放，包括那些结论已经死掉的脚本、跑出 nan 的日志、以及推翻自己结论的审计报告。

---

## 一、时间线

### V3 · 2026-08-19 — 分界线

| 日期 | 事件 | 状态 |
|---|---|---|
| 08-19 | **Survival-First AI（V3 中英双版）** 公开，DOI `10.5281/zenodo.22009445` | 已公开 |
| 08-20 | White-Box Reproduction Protocol（2×2 正交分离），DOI `10.5281/zenodo.22031438` | 已公开 |

**以下全部长期未公开。**

### 早期对照 · 08-23 ~ 08-24 → `05_早期对照实验_0823至0825/`

| 日期 | 实验 | 结果 | 状态 |
|---|---|---|---|
| 08-23 | `exp1` 真实幻觉 / `exp1b` 分层 / `exp2` 层扫描 / `exp4` 五类任务 | 四维信号对数字类近乎无判别力（dev 0.678 vs 0.693，**熵还反向** 2.75 vs 2.59） | 部分已被自己推翻 |
| 08-23 | `verify_duality.py` 命题 1/2 感应内容对偶 | 恒等式"完全验证" | ⚠️ **恒等式无死法**：反例只会是 bug，不构成可证伪命题 |
| 08-24 | `exp5` 五觉 / `exp6` gate upgrade / `exp7`、`7b`、`7c` gate flow / `exp8` imp layers | S_var 0.551 / S_err 0.752 / S_imp 0.365 | 探索性结果 |
| 08-24 | `lly_suite v1.0→v1.1`、`lly_lci`、`lly_attn_diag`、`lly_erank` | 数字子空间 AUC 0.782 | 探索性结果 |
| **08-24 12:40Z** | **`baiheyanzheng-2x2` 仓库公开** | — | 🔴 **分水岭**：此后任何"先出结果、后补声明"的做法都不具备证据资格 |

### 心层迭代 v4 系列 · 08-25 → `01_心层迭代_v4系列/`

| 日期 | 版本 | 结果 |
|---|---|---|
| 08-25 | `lly_heart_layer_v1.0` → `v4_heartgate` → `v41_ringgate` → `v42` | 四次迭代，run2–run9 八轮日志 |
| 08-25 | **`exp9_h_dev_independence`** | 🔴 **证伪 S_surv**——标签泄漏。此前宣称的 S_surv 0.936 / 垂危区 85% 双双作废 |
| 08-25 | `lly_orbit_*`、环道实验、`lly_heart_brain`、命题 1/2 验证账目 | 探索性结果 |

> **exp9 是本段最重要的一次失败。** 它证明"心"的内部信号里混进了标签信息——
> 这正是后来"心只看触发器、裁判权交给域"这一职责划分的直接来源。

### v5.0 与 prover · 08-27 ~ 08-28 → `02_心层_v5.0原始实验/`、`04_prover长文本实验/`

| 日期 | 事件 | 状态 |
|---|---|---|
| 08-27 | `lly_heart_v5.py`（82,522 B / 1805 行）+ `lly_run10.log` | 原始实验 |
| 08-27 | `lly_prover` 长文本实验（中/英/多模型） | 原始实验 |
| 08-28 | `lly_heart_v5_15b.py` + `run11.log` + `lly_heart_report.json` | **对外宣称"幻觉检出率 100%"** |
| 08-28 UTC | 三个 Zenodo 封印 + tag `data-freeze-20260829` | 数据边界封印 |

### 独立审计 · 08-29 → `03_心层_v5.0独立审计/`

| 时间 | 事件 |
|---|---|
| 08-29 11:47–11:54 | **在服务器上实际重跑 v5.0 + 独立构造三组对照 + 逐行代码审计** |

**判决：v5.0 不成立。** 三条独立证据：

```
1) 无判别力   错误数字 68.8% 被判 HALLUC
              正确数字 56.4% 被判 HALLUC
              ─────────────────────────
              只差 12.3 个百分点
              按 v5.0 自己的 OR 口径：12 条正确事实里 10 条被误报（83.3%）

2) 不可复现   同代码同数据两次运行
              总体准确率 64.5% vs 87.4%   （差 22.9 pt）
              heart_mlp   24%  vs 76%     （run11 出现 nan）
              代码无随机种子

3) 九处结构性缺陷
              其中 NEUTRAL 类在评分函数里根本没有任何分支能判对
              0/22 = 0% 不是模型问题，是评分 bug
```

**OR 口径已饱和失效**：OR 运算 + 每条 2–5 个数字 token，随机基线就有 **87.5%**。
一个把 83% 正确事实都报错的系统，其"100% 检出率"不携带任何信息。

**复现性来源拆分（关键修正）**：一度归因于幂迭代不收敛，实测后推翻——

| 来源 | 波动幅度 | 判定 |
|---|---:|---|
| 训练洗牌 | **±26 pt** | ✅ 元凶 |
| 幂迭代（`n_iter=10`） | 0.06% – 6.7% | ❌ 量级差两个数量级，不是主因 |

附带发现：`sense_analyze(n_iter=...)` 是**死参数**，从未向下传递；源码第 890 行硬编码 `n_iter=10`，而默认值写作 20。

### 病因定位与仂域 · 08-29 → `06_仂域_数字域_v1/`

| 时间 | 事件 |
|---|---|
| 08-29 | **自参照病因定位**：`R = R(阴, 阳, 感应历史)`（卷四 §五）⇒ I 无量纲、尺度不变 ⇒ 只能测相对异常，测不了绝对真假 |
| 08-29 | 职责边界确立：**幻觉不看心，幻觉看域**。心 = 触发器（召回率，内生合法）；域 = 裁判（精确率 + 可纠性，必须外置锚） |
| 08-29 13:00–13:43 | **仂域数字域 v1 建成并跑通**：10 锚点 × 4 错型 × 4 口径 × 3 遍 |

**仂域 v1 结果**（40 例，全量逐位一致）：

| 档 | L（对数偏离） | 探针 | 抗扰 | 回纠 | Δ（概率优势比） |
|---|---:|---:|---:|---:|---:|
| L0 对照 | 0.000 | 60% | **100%** | 100% | — |
| D1 偏一位 | 0.015 | 60% | **30%** | 60% | −1.913 |
| D2 偏两位 | 0.155 | 60% | **20%** | 70% | −1.697 |
| M 数量级 | 1.000 | 60% | **10%** | 20% | −2.996 |

```
⇒ R_抗扰 < L ≈ 0.015（小于 1.04 倍偏离即失守）
⇒ 模型自身 R = 0：它没有自己的参照，只能顺着上下文走
```

**反直觉发现**：已知组抗扰 17% < 未知组 50%——**知道答案反而更容易被带跑**。
**唯一例外**：π，D1/D2/M 全纠回，Δ 全为正（+2.00 / +1.58）。

**由此提出的修正**：`R_数字域` 应改写为 `R(域, 锚点, 模型)`——R 不是模型的属性，是三者的关系。

> 注：上表 D1/D2 的 L 值（0.015 / 0.155）为当时笔误。以 `result_v1.json` 原始数据为准：
> **D1 L中位 = 0.0024，D2 L中位 = 0.0414**（M = 1.0001）。两版实验同域同案例，L 相同。

### 1.5B-Instruct 复测（P0 的执行）· 08-29 15:33–17:33 → `06_仂域_数字域_v1/result_v1_15b.md`

| 时间 | 事件 |
|---|---|
| 08-29 15:33–17:33 | **Qwen2.5-1.5B-Instruct 全量重跑**：同 40 例 × 4 口径 × 3 遍，fp16 CPU，约 2 小时 |
| 08-29 17:40 | **判定缺陷发现**：`judge()` 的裸子串兜底 `or (wrong_surf in text)` 把 M 档正确输出误判为污染（错误值是真值前缀，"10"⊂"100"）。仅影响 M 档 × 3 文本口径；两版同代码同影响，对照仍同尺。原始 JSON 原样保留，数值级重判见 `rejudge_m.py` |

**1.5B 结果**（M 档为修正后数字；复现性 40/40 逐位一致）：

| 档 | 探针 | 抗扰 | 回纠 | Δ 均值 |
|---|---:|---:|---:|---:|
| L0 对照 | **100%** | 40% | 80% | — |
| D1 偏一位 | **100%** | **30%** | 30% | −1.397 |
| D2 偏两位 | **100%** | **30%** | 40% | −1.568 |
| M 数量级 | **100%*** | **30%*** | 30%* | −2.293 |

```
⇒ R ≈ 0 跨模型复现：探针 10/10 全知道，抗扰三档 30/30/30 无一过 50%，Δ 三档全负
⇒ 抗扰曲线修正后不是"随 L 递减的斜坡"，是"有错即跌的平台"（污染是开关，不是旋钮）
⇒ 0.5B 的 L0=100% 证实为 base 复读伪影（1.5B 不复读，L0 抗扰 40%）
⇒ π 例外跨模型复现：仍唯一三档全纠回（Δ +0.31 / +0.49）
```

---

## 二、本目录已死的结论（明确列出，不掩饰）

| 结论 | 死因 | 证据位置 |
|---|---|---|
| S_surv 0.936 / H 垂危区 85% | exp9 证明标签泄漏 | `05_.../scripts/exp9_h_dev_independence.py` |
| v5.0 幻觉检出率 100% | 无判别力（误报 83.3%）+ 不可复现 | `03_心层_v5.0独立审计/00_v5.0验证报告.md` |
| 心可独立判别幻觉 | 自参照：内生量只能测相对异常 | `06_仂域_数字域_v1/00_仂域定义.md` §8 |
| 五条预注册（08-28） | 数据先行 4 天、公开先行 3–4 天 ⇒ HARKing | `Zenodo封印账目_20260829.md`（仓库根） |

> 两个仓库的 README 仍宣称 S_surv 0.936 / 垂危区 85%，**已知被证伪，暂未改动**。
> 此处如实记录，不做悄悄修改。

---

## 三、指标口径的修正记录

研究过程中发现并修正的两处度量缺陷，保留在 `06_仂域_数字域_v1/` 的实现里：

1. **相对偏离 `rel` 有 100% 上界**——8848→884 差一个数量级，`rel` 却只有 90%。
   ⇒ 改用**对数比值** `L = log10(max(given/true, true/given))`，M 档精确落在 L = 1.000。
2. **命中容差 2% 过宽**——D1 档偏离仅 0.06%，2% 容差会把"偏一位"与"完全正确"判为同类。
   ⇒ 改为 `1e-6`。

---

## 四、如何验证本目录

```bash
# 1. 复现 v5.0 的失败（需要 torch + transformers + 一台 2C3.6G 的机器）
python 03_心层_v5.0独立审计/verify_v5_clean.py

# 2. 复现收敛性实验（n_iter=10 vs 50）
python 03_心层_v5.0独立审计/repro_conv_test2.py

# 3. 复现仂域数字域 v1（40 例 × 4 口径 × 3 遍，应 40/40 逐位一致）
python 06_仂域_数字域_v1/leyu_probe_v1.py
python 06_仂域_数字域_v1/analyze_v1.py
```

三个脚本均已固定随机种子（`random.seed(42)` / `torch.manual_seed(42)`）。

---

## 五、2026-08-29 晚 · 仂域门控架构 v1（`07_仂域门控架构_v1/`）

架构（按指令：**门控决策本身也在仂域内**）：

```
任务 → ①仂域求存 → 分词 → 嵌入 → 神经网络（干净，不插头）→ 输出
     → ②仂域门控 → 行 / 待 / 撤    待 = 重问回主干，二轮再过门控
```

**关键认识**：仂域本质是**数据库**（10 条锚点记录，只负责存与供），
判定规则外挂、不写在库里。所谓"纠错度"是纠错动作投射到域上的投影，不是模型的属性——
这解释了为什么 `R ≈ 0`：模型里本来就没有可测的纠错度。
五道纠错度 = 强度 / 速度 / 方向 / 空间 / 时间，当前判定**只用了强度一道**。

两轮实验（40 例 × 3 遍）：

| 轮次 | 问题 | 结果 |
|---|---|---|
| 首轮 `max_new=24` | 生成被截断（对 baseline 不利、门控不受影响）+ "千米"的"千"被当 1000 | **数字虚高，已作废** |
| 修正后 `max_new=96` | 截断解除 + 单位词剥离 | 提升例数 **23 → 16** |

**修正后的核心发现**：按门控动作拆开，成色差异极大——

| 动作 | 次数 | 成功 | 成功率 |
|:--:|---:|---:|---:|
| 撤（域直接供值，不过模型） | 39 | 39 | **100%** |
| 行（域判断没问题） | 63 | 57 | 90%（6 例漏检） |
| 待（重问，让模型自己再来） | 18 | **0** | **0%** |

> **凡要求模型自己纠的，一律 0 分；凡域直接给答案的，一律满分。**
> 门不值钱，值钱的是门后那条通向数据库的旁路。这是 `R ≈ 0` 在架构层面的再次复现。

发现的问题（同页记录）：
- 量道阈值 `L < 0.003` 太松，黄河长度 5461 vs 5464（L=0.0002）被放行，漏检 6 例×遍
- `classify()` 的 `max()` 抹掉了方向信息（偏大/偏小同 L）——五道里的"方向"整道丢失
- 8 例失败全部集中在 D1/D2 档；M 档零失败（越界必触发撤）

已公开：`leyu-number-domain-v1`（码云 + GitHub 同名仓库）。

---

## 六、2026-08-30 · Zenodo 归档发布

| 项 | 值 |
|---|---|
| 标题 | 「仂庸」 —— 仂域外置锚纠错与门控架构实验数据(2026-08-29)· 中和道化论 |
| **DOI** | **10.5281/zenodo.22164037** |
| Concept DOI | 10.5281/zenodo.22164036 |
| 页面 | https://zenodo.org/records/22164037 |
| 许可 | CC-BY-4.0 |
| 归档文件 | `leyu_v1_zenodo.zip`，187,742 B，31 项 |
| MD5 | `c4f045b615202aae9442ecb7baa18ace` |
| SHA256 | `11dcf415b72b356be14e21af3f0ea4e06d565c3b55acadf03ea98509bb8544e0` |

**署名与归属**（随记录一并发布）：
- 体系：中和道化论 · 子体系：「仂庸」 · 作者：何一（独立研究者）
- 「仂庸」主干已冻结（心与域职责分离、自参照根因、`R(域, 锚点, 模型)` 的定义不再变动），本工作为主干之上的实验验证与工程实现，不构成对主干的修改
- 方案由作者提供，AI 提供辅助（代码实现、批量运行、数据整理、图表绘制、文稿起草）；AI 不参与方案设计与结论裁定，不作为作者署名

**过程中的两次返工**（同页记录）：
1. 文件上传首试 HTTP 400 —— Zenodo 不收裸 `octet-stream` POST，改用 bucket `PUT` 方成
2. 引号样式返工 —— 中文弯引号 `“”` 被 Zenodo 强制归一化为 ASCII `"`，改用直角引号 `「」` 才守住

**有意保留裸名的两处**：`仂庸结构图_20260830.svg`（文件名，加引号会断引用）、时间线中 `"仂庸是方向"`（整句引用，拆开会坏语义）。

发布后经匿名下载校验：MD5 与本地一致，DOI 解析正常。

---

*本文件为索引，非结论。所有判断以各子目录内的原始数据为准。*
