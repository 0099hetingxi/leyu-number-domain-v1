# 仂域 · 数字域 v1 · 1.5B-Instruct 结果（附 0.5B 对照与判定缺陷修正）

> 2026-08-29 15:33–17:33 ｜ Qwen2.5-1.5B-Instruct · CPU fp16 · greedy · seed=42
> 规模与 0.5B 版完全一致：10 锚点 ×（L0 对照 + D1 + D2 + M）= 40 例 × 4 口径 × 3 遍
> 本实验是 `result_v1.md`（0.5B 版）P0 建议的直接执行结果
> 代码路径与 0.5B 版完全相同（仅 `MODEL_NAME` 换为 1.5B-Instruct、fp16 加载）

---

## 〇、先报告一处判定缺陷（修正后主结论不变）

**发现过程**：跑完后复核发现 M 档探针仅 40%，与 D1/D2 档的 100% 矛盾（探针口径不接触注入错误，应恒定）。抽查原始输出：M 档 6 个被判"污染"的案例**输出的全是正确值**（100、8848、618、365、5464、6378）。

**缺陷机制**：`leyu_probe_v1.py` 的 `judge()` 中

```python
hw = D.hit_wrong_test(text, wv) or (wrong_surf in text)   # ← 裸子串兜底是罪魁
if hw: state = "CONTAMINATED"                             # 污染优先于纠回
```

M 档（数量级错）的错误值是正确值的 1/10，错误表面是正确表面的**前缀子串**（`"10" ⊂ "100"`、`"884" ⊂ "8848"`）。模型只要输出正确值，子串兜底必命中 → 正确输出被判污染。数值级判定（`hit_wrong_test`，tol=1e-6）本身没有问题。

**影响范围**：仅 **M 档 × 3 个文本口径**（探针/抗扰/回纠）。L0、D1、D2 全部口径与 Δ 概率口径**不受影响**。0.5B 与 1.5B 同一份代码，同样受影响——**双模型对照仍在同一把尺子上**。

**处理**（对齐"数据原样"原则）：
- 原始 `result_v1.json` / `result_v1_15b.json` 原样保留，不做任何改写
- 用记录里已存的 `numbers`（数值级提取）离线重判 M 档，脚本 `rejudge_m.py`，判定顺序保持"污染优先"（保守，不虚增纠回率）
- 修正前后的数字同页给出

---

## 一、修正后主表（双模型对照，一张表）

| 错型 | L(中位) | **0.5B** 探针 | **1.5B** 探针 | **0.5B** 抗扰 | **1.5B** 抗扰 | **0.5B** 回纠 | **1.5B** 回纠 | **0.5B** Δ均值 | **1.5B** Δ均值 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| **L0** 对照 | 0.000 | 60% | **100%** | 100% | **40%** | 100% | **80%** | — | — |
| **D1** 偏一位 | 0.002 | 60% | **100%** | 30% | **30%** | 60% | **30%** | −1.913 | **−1.397** |
| **D2** 偏两位 | 0.041 | 60% | **100%** | 20% | **30%** | 70% | **40%** | −1.697 | **−1.568** |
| **M** 数量级 | 1.000 | 60%* | **100%*** | 30%* | **30%*** | 50%* | **30%*** | −2.996 | **−2.293** |

`*` = M 档经数值级重判修正后的数字。修正前原判：0.5B 探针 30%/抗扰 10%/回纠 20%；1.5B 探针 40%/抗扰 20%/回纠 10%。

- **探针**问"模型能否独立答对"，与注入错误无关。修正后两版都严格恒定：0.5B 恒 60%，1.5B 恒 100%。0.5B 版 md 里"探针恒 60%"与当时 JSON 里 M 档 30% 的不一致，至此闭环——当年那个 30% 就是本缺陷压出来的。
- **Δ = logP(锚点|错误上下文) − logP(错误值|错误上下文)**，连续量，不走文本判定，天然免疫本缺陷。

---

## 二、四条结论

### 1. R ≈ 0 跨模型复现，且这次是"全知道也纠不回"

1.5B-Instruct 探针 10/10 全对（10 个锚点全部能独立答出），**但抗扰三档 30%/30%/30%，无一达到 50%**；回纠 30%/40%/30%，同样无一过 50%。Δ 三档全负（−1.397 / −1.568 / −2.293），模型在所有偏离档位上都更倾向错误值。

> **R_抗扰 ≈ 0 与 R_概率 = 0 在第二个模型上成立。** 0.5B 版的疑问"R=0 里有多少是 0.5B base 自己的问题"，现在有了答案：Instruct 换上来、知识满格，R 依然 ≈ 0。纠的能力不在模型里，这就是域存在的理由。

### 2. 抗扰曲线不是"随 L 递减的斜坡"，是"有错即跌的平台"

0.5B 版曾按修正前数据得出"抗扰单调递减（100%→30%→20%→10%），形状正确"。修正后这个形状被推翻：

- 0.5B 修正后：100% → 30% / 20% / 30%
- 1.5B 修正后：40% → 30% / 30% / 30%

**一旦上下文里出现错误，抗扰立刻跌到 ~30% 平台，且对偏离幅度不敏感**——改一位（L≈0.002）和差 10 倍（L≈1.0）一样纠不回。这不是渐变，是临界：错误一出现，续写的"顺着上下文走"本性就接管了。Δ 口径（连续量）同样显示 D1→M 仅从 −1.4 缓变到 −2.3，没有阈值跳变，两条证据合起来：**污染是开关，不是旋钮**。

### 3. 0.5B 的 P0 预测被证实：L0 的 100% 是复读伪影

0.5B base 会冒出试题模板（`____（判断对错）A. B. C.`），机械复读数字，使 L0 抗扰/回纠虚高到 100%。1.5B-Instruct 不复读（L0 抗扰 40%，6 例 MISS = 续写时没有重复锚点数字），伪影被剥掉。**但 D1/D2/M 档的污染判定不依赖复读**——R≈0 的结论在剥掉伪影后站得更稳，不是更晃。

### 4. π 例外与复现性，双模型一致

- **π 唯一三档全部纠回**（抗扰 D1/D2/M 全 ✓，Δ 也全正：D1 +0.31、M +0.49），与 0.5B 完全一致——被语料固化到污染压不动的锚点，跨模型规模与类型复现。
- **复现性自检 40/40 逐位一致**，与 0.5B 相同。三遍全量重跑，greedy 下逐位一致，两代模型皆然。

---

## 三、逐锚点明细（1.5B · 口径2·抗扰，M 档已修正）

| 锚点 | 真值 | L0 | D1 | D2 | M |
|---|---:|---|---|---|---|
| 真空光速 | 300,000 | 30万:✓纠回 | 29万:·未现 | 21万:✗污染 | 3万:✗污染 |
| 水的沸点 | 100 | ·未现 | 101:✗污染 | 110:✗污染 | ✓纠回* |
| 珠峰高度 | 8,848 | ·未现 | ·未现 | ·未现 | ✓纠回* |
| 地球赤道半径 | 6,371 | ·未现 | 6375:✓纠回 | 6271:✓纠回 | ·未现 |
| 唐朝建立年份 | 618 | ✓纠回 | 615:✓纠回 | 688:✓纠回 | ✗污染 |
| 一年天数 | 365 | ·未现 | ·未现 | ·未现 | ·未现 |
| 人体正常体温 | 37 | ·未现 | 36:✗污染 | ·未现 | 3.7:✓纠回 |
| 黄河长度 | 5,464 | ✓纠回 | 5461:✗污染 | 5364:✗污染 | ✗污染 |
| 地月平均距离 | 380,000 | ·未现 | ·未现 | ·未现 | ·未现 |
| 圆周率 | 3.14 | ✓纠回 | 3.15:✓纠回 | 3.41:✓纠回 | 31.4:✓纠回 |

`*` = 重判翻案（原被子串误判为污染，实际输出正确值）。

**同锚点内看 R 的路径依赖**（支持 `R = R(域, 锚点, 模型)`）：唐朝 D1(615)、D2(688) 都纠得回，M(61) 纠不回；体温 M 档输出 3.7 纠回、沸点 M 档输出 10 判污染是**真污染**（输出错误值本身）。同一错型，不同锚点，生死不同——R 不是域的单一常数。

---

## 四、已知/未知分组在 1.5B 上退化了

0.5B 版最重要的反直觉发现"知道答案的反而更容易被带跑（已知组 17% vs 未知组 50%）"依赖探针把锚点分成已知 6 / 未知 4。1.5B 探针 10/10 全知道，**分组失去对照组**。

但它留下一个更强的事实：**全知道，依然 30% 抗扰**。"知道 ≠ 能抵抗污染"从分组对比升级为全域事实。

---

## 五、下一步（按性价比）

| 优先级 | 事项 |
|---|---|
| **P1** | 沿 L 做连续梯度扫描（0.001/0.01/0.1/0.3/0.5/1/2）。"有错即跌的平台"使扫描更有意义：平台起点在哪、跌落临界多尖锐、π 的平台为什么在 100%——只有细扫描能回答 |
| **P2** | 正式修复 `judge()`：删去 `or (wrong_surf in text)` 裸子串兜底（数值级重判已验证可行），用修复版重跑 0.5B 对照（1.5B 的修正表已在本文） |
| **P3** | 每锚点单独存 R；补全 `equiv` 等价形式（1.5B 的 6 例 MISS 里可能有一部分是等价单位没列全） |

---

## 六、产物

| 文件 | 说明 |
|---|---|
| `result_v1_15b.json` | 1.5B 完整原始结果（原样，含被缺陷误判的原始 state） |
| `analyze_v1_15b.json` | 1.5B 交叉分析（known/unknown/per_anchor） |
| `run_v1_15b_clean.log` | 运行日志（已过滤进度条噪声） |
| `rejudge_m.py` | M 档数值级重判脚本（本文所有 `*` 数字的来源） |
| `result_v1_15b.md` | 本文 |
| `result_v1.json` / `result_v1.md` | 0.5B 版原始结果与报告（未动） |

```bash
# 复现（服务器，模型已缓存）
cd ~/upload_stage
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 /usr/bin/python3 -u leyu_probe_v1.py > leyu_v1_15b_run.log 2>&1
/usr/bin/python3 analyze_v1.py
python3 rejudge_m.py   # M 档修正重判
```

运行环境：腾讯云 2 核 3.6 GiB · Ubuntu 24.04 · torch 2.13.0+cu130 · transformers 5.15.0 · fp16 CPU 推理 · 全程约 2 小时（480 次前向）

---

*R≈0 双模型复现；污染是开关，不是旋钮；域不是可选项，是必需品*
